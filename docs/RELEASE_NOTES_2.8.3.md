# Codex Goal Supervisor 2.8.3

## Verified Candidate Baseline

- Convert the public capabilities introduced from 2.5 through 2.8.2 into a
  reproducible business matrix covering source, extracted packages, physical
  editions, and the fixed real Luna Max task.
- Repair confirmed candidate defects in Goal replacement hooks, dependency
  interpretation, procedure-memory fallback, reuse classification, roadmap
  runtime roots, and unknown-project alignment.
- Keep publication after source, extracted-package, distribution, and real
  black-box validation. This candidate is not published by its build step.

## Structured Phased Goals

- Keep one concise confirmed North Star and add a shallow project program
  outline for super-complex work.
- Detail only the current dependency-ready phase and project its exact
  2,000-3,500 character objective into native Goal mode.
- Require distinct current reuse research at outline and phase boundaries.
- Keep phase outcomes between 2 and 24 hours, while leaving smaller work as an
  action inside the current phase.
- Run declared validation-catalog IDs before phase completion. Failed
  validation leaves the phase active and prevents advancing its dependents.
- Record predicted and actual duration, first product action, first valid
  evidence, attempts, and completion locally without enabling feedback upload.
- Preserve legacy `phase-set`, `phase-complete`, and `phase-advance` behavior
  for existing project state.
